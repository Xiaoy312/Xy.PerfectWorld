<mah:MetroWindow x:Class="Xy.PW.SettingView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:local="clr-namespace:Xy.PW"
                 mc:Ignorable="d"
                 Title="Setting"
                 x:Name="Setting">
    <mah:MetroContentControl>
        <mah:MetroAnimatedSingleRowTabControl>
            <TabItem Header="Client">
                <DataGrid ItemsSource="{Binding RunningGames}"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          SelectedValue="{Binding SelectedGame}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="PID" Binding="{Binding Process.Id}" />
                        <DataGridTextColumn Header="GameStatus" Binding="{Binding Status}" />
                        <DataGridTextColumn Header="Player" Binding="{Binding Player}" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Hack" />
            <TabItem Header="Auto" />
        </mah:MetroAnimatedSingleRowTabControl>
    </mah:MetroContentControl>
</mah:MetroWindow>
